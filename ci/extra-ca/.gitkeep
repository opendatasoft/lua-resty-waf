Drop .crt files here only if your network MITMs TLS or blocks port 80.
They are baked into the image's CA bundle before apt runs. Ignored by
git: this is local network infrastructure, not project configuration.
